package com.hellofresh.challenge.entitys;

/**
 * Enum for different payment options in order process, since id is there and
 * only lower cased, enum is enough
 * 
 * @author benja
 *
 */
public enum Payment {
	BANKWIRE, CHEQUE
}
